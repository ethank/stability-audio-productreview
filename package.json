{
  "name": "stability-audio-product-review",
  "version": "0.1.0",
  "private": true,
  "description": "Hosted Monday product review app for Stability Audio.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "migrate": "node scripts/migrate.js",
    "verify-db": "node scripts/verify-db.js",
    "hash-password": "node scripts/hash-password.js",
    "check": "node --check server.js && node --check app.js && node --check scripts/migrate.js && node --check scripts/verify-db.js && node --check scripts/hash-password.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@auth/express": "^0.12.2",
    "express": "^5.2.1",
    "pg": "^8.16.3"
  }
}
